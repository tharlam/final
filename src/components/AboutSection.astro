<section id="about" class="about-us-section">
  <div class="container">
    <h2 class="section-title" data-aos="fade-up" data-i18n="section_title">
      About Us
    </h2>
    <div class="about-content-grid">
      <div class="about-text" data-aos="fade-left">
        <p data-i18n="about_intro_paragraph">
          Tharlam Academy is a unique school dedicated to an education that
          seamlessly blends traditional Buddhist values with a modern,
          progressive curriculum. Our mission is to nurture well-rounded
          individuals who possess both intellectual prowess and a strong moral
          compass. The academy is providing a holistic learning environment that
          encourages critical thinking, creativity, and compassion.
        </p>
        <p data-i18n="about_vision_paragraph">
          Our vision is to empower the next generation of global citizens with
          the wisdom of the past and the skills of the future. We foster an
          atmosphere of respect, discipline, and ethical conduct, preparing
          students to face the challenges of the modern world while staying true
          to their spiritual heritage.
        </p>
        <div class="about-cta">
          <a
            href="/final/about"
            class="btn primary-btn"
            data-i18n="about_learn_more_button"
          >
            Learn More About Us
          </a>
        </div>
      </div>
      <div class="about-image" data-aos="fade-up">
        <img
          src="/final/house.webp"
          alt="about image alt"
          class="responsive-image"
          data-i18n-alt="about_image_alt"
        />
      </div>
    </div>
  </div>
</section>

<style>
  /* ======================================================= */
  /* --- Base Styles (Desktop) --- */
  /* ======================================================= */

  .about-us-section {
    padding: 50px 0;
    background-color: #fff;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 10px;
  }

  .section-title {
    font-size: 38px;
    font-weight: 700;
    color: #001f3f; /* Deep Blue */
    text-align: center;
    margin-bottom: 30px;
  }

  .about-content-grid {
    display: flex;
    flex-wrap: wrap;
    gap: 50px;
    align-items: flex-start;
    justify-content: center;
  }

  .about-text {
    flex: 2;
    min-width: 300px;
  }

  .about-text p {
    font-size: 17px;
    line-height: 1.8;
    margin-bottom: 20px;
    color: #555;
    /* --- JUSTIFICATION UPDATES START --- */
    text-align: justify;
    text-justify: inter-word; /* Creates even spacing between words */
    hyphens: auto;            /* Prevents big gaps on smaller lines */
    /* --- JUSTIFICATION UPDATES END --- */
  }

  .about-text p strong {
    font-weight: bold;
    color: #333;
  }

  .about-image {
    flex: 1;
    min-width: 280px;
    text-align: center;
  }

  .about-image .responsive-image {
    max-width: 100%;
    height: auto;
    border-radius: 8px;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
  }

  .about-cta {
    margin-top: 0;
    text-align: left;
  }

  .btn {
    display: inline-block;
    padding: 12px 30px;
    font-size: 16px;
    font-weight: 600;
    text-decoration: none;
    border-radius: 5px;
    transition: background-color 0.3s ease, transform 0.2s ease;
  }

  .primary-btn {
    background-color: #001f3f;
    color: white;
    border: 2px solid #001f3f;
  }

  .primary-btn:hover {
    background-color: black;
    border-color: black;
    transform: translateY(-2px);
  }

  /* ======================================================= */
  /* --- RESPONSIVENESS --- */
  /* ======================================================= */

  @media (max-width: 992px) {
    .about-us-section {
      padding-top: 50px;
      padding-bottom: 50px;
    }
    .section-title {
      font-size: 32px;
    }
    .about-content-grid {
      gap: 30px;
    }
    .about-text, .about-image {
      min-width: unset;
    }
  }

  @media (max-width: 768px) {
    .about-us-section {
      padding-top: 30px;
      padding-bottom: 30px;
    }
    .about-content-grid {
      flex-direction: column;
      align-items: center;
      gap: 20px;
    }
    .about-text {
      order: 2;
    }
    .about-image {
      order: 1;
      margin-bottom: 20px;
    }
    .section-title {
      font-size: 28px;
    }
    .about-text p {
      font-size: 16px;
      /* Keeping it justified on tablet/mobile for consistency */
      text-align: justify; 
    }
  }

  @media (max-width: 480px) {
    .about-us-section {
      padding-top: 20px;
    }
    .section-title {
      font-size: 24px;
    }
    .about-text p {
      font-size: 15px;
      /* On very small phones, you can change to 'left' if justify looks too spaced out */
      text-align: justify;
    }
  }
</style>