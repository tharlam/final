<section class="sports-hero">
    <div class="hero-content">
        <h1>Tharlam Sports Association</h1>

        <p>
            A core part of the **Tharlam Monastery's** commitment to holistic
            development, the Sports Association offers multiple programs,
            fostering **teamwork, discipline, and healthy living** among our
            students. Our primary goal is to nurture both academic and athletic
            potential within the monastic community.
        </p>

        <a href="#programs" class="view-programs-btn">View Our Programs →</a>
    </div>
</section>

<style>
    .sports-hero {
        /* Set the background image and create a dark overlay */
        background-image: 
        /* Dark Overlay using a linear gradient over the image */
            linear-gradient(rgba(0, 0, 0, 0.7), rgba(0, 0, 0, 0.7)),
            /* Your background image (replace the path) */
                url("football.webp");

        /* Ensure the image covers the entire section */
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;

        /* Set the height and use Flexbox to center the content */
        min-height: 500px; /* Adjust height as needed */
        display: flex;
        justify-content: center;
        align-items: center;
        text-align: center;

        /* Padding for mobile responsiveness */
        padding: 20px;
        color: white; /* Text color is white */
    }

    .hero-content {
        /* Limit the width of the content for better readability */
        max-width: 800px;
        margin: 0 auto;
    }

    /* Title Styling */
    .sports-hero h1 {
        font-size: 3rem; /* Adjust font size */
        margin-bottom: 20px;
    }

    /* Paragraph Styling */
    .sports-hero p {
        font-size: 1.1rem;
        line-height: 1.6;
        margin-bottom: 30px;
    }

    /* Button Styling */
    .view-programs-btn {
        /* Button appearance */
        display: inline-block;
        padding: 12px 25px;
        border: 1px solid white; /* White border */
        border-radius: 5px;
        background-color: transparent;
        color: white;
        text-decoration: none; /* Remove underline */

        /* Transition for hover effect */
        transition:
            background-color 0.3s,
            color 0.3s;
    }

    /* Button Hover Effect */
    .view-programs-btn:hover {
        background-color: white;
        color: #000; /* Text turns black on hover */
    }
</style>
