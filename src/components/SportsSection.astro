<section class="sports-hero" id="sports-hero">
    <div class="hero-content">
        <h1 data-i18n="sports_hero_title">Tharlam Sports Association</h1>

        <p data-i18n="sports_hero_description">
            A core part of the <strong>Tharlam Monastery's</strong> commitment to
            holistic development, the Sports Association offers multiple programs,
            fostering <strong>teamwork, discipline, and healthy living</strong> among
            our students. Our primary goal is to nurture both academic and athletic
            potential within the monastic community.
        </p>

        <div class="hero-cta">
            <a
                href="#programs"
                class="btn primary-btn-outline"
                data-i18n="sports_hero_button"
            >
                View Our Programs →
            </a>
        </div>
    </div>
</section>

<style>
    /* ======================================================= */
    /* --- Sports Hero Styles (Consistent with About Section) --- */
    /* ======================================================= */

    .sports-hero {
        background-image:
            linear-gradient(rgba(0, 0, 0, 0.7), rgba(0, 0, 0, 0.7)),
            url("/final/football.webp"); /* Updated path to match your /final/ convention */
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;

        /* Consistency: Matching your vertical spacing logic */
        min-height: 300px;
        display: flex;
        justify-content: center;
        align-items: center;
        text-align: center;
        padding: 20px 20px;
        color: white;
    }

    .hero-content {
        max-width: 1200px;
        margin: 0 auto;
    }

    .sports-hero h1 {
        /* Consistency: Using your Heading scale */
        font-size: 38px;
        font-weight: 700;
        margin-bottom: 20px;
        color: white;
    }

    .sports-hero p {
        /* Consistency: Using your Paragraph scale (17px) and line-height (1.8) */
        font-size: 17px;
        line-height: 1.8;
        margin-bottom: 30px;
        color: #eee; /* Slightly lighter for readability on dark backgrounds */
    }

    .sports-hero p strong {
        color: white;
        font-weight: bold;
    }

    /* --- Button Consistency --- */
    .btn {
        display: inline-block;
        padding: 12px 30px;
        font-size: 16px;
        font-weight: 600;
        text-decoration: none;
        border-radius: 5px;
        transition:
            background-color 0.3s ease,
            transform 0.2s ease;
    }

    /* Outline version of your primary-btn for Hero overlay */
    .primary-btn-outline {
        background-color: transparent;
        color: white;
        border: 2px solid white;
    }

    .primary-btn-outline:hover {
        background-color: white;
        color: black;
        transform: translateY(-2px);
    }

    /* ======================================================= */
    /* --- RESPONSIVENESS (Matching About Us Section) --- */
    /* ======================================================= */

    @media (max-width: 992px) {
        .sports-hero {
            padding: 50px 20px;
        }
        .sports-hero h1 {
            font-size: 32px;
        }
    }

    @media (max-width: 768px) {
        .sports-hero {
            padding: 30px 20px;
            min-height: 400px;
        }
        .sports-hero h1 {
            font-size: 28px;
        }
        .sports-hero p {
            font-size: 16px;
        }
    }

    @media (max-width: 480px) {
        .sports-hero {
            padding: 20px 20px;
        }
        .sports-hero h1 {
            font-size: 24px;
        }
        .sports-hero p {
            font-size: 15px;
        }
    }
</style>
