<section class="legacy-section site-section">
    <div class="legacy-content">
        <h1 data-i18n="sports_legacy_title" class="section-title">
            Tharlam Sports Association
        </h1>
        <p
            data-i18n="sports_legacy_description"
            class="consistent-paragraph-text"
        >
            A core part of the **Tharlam Monastery's** commitment to holistic
            development, the Sports Association offers multiple programs,
            fostering **teamwork, discipline, and healthy living** among our
            students. Our primary goal is to nurture both academic and athletic
            potential within the monastic community.
        </p>
        <a href="/final/sport" class="join-button">
            <span data-i18n="sports_legacy_cta_text">View Our Programs</span>
            <span class="arrow">&rarr;</span>
        </a>
    </div>
</section>

<style>
    /* --- BASE STYLES (Desktop/Default) --- */

    .legacy-section {
        background-image: linear-gradient(
                rgba(0, 0, 0, 0.6),
                rgba(0, 0, 0, 0.6)
            ),
            url("/final/football.webp"); /* Assuming /public/football.jpeg */
        background-size: cover;
        background-position: center;
        color: white;
        /* CONSISTENCY: Vertical padding is now handled by the global .site-section class. Removed 80px */
        padding: 0 20px;
        text-align: center;
    }

    .legacy-content {
        max-width: 900px;
        margin: 0 auto;
    }

    /* H1 is now using .section-title class for consistent sizing */
    .legacy-section h1 {
        /* REMOVED: font-size (Now handled by .section-title) */
        /* REMOVED: font-weight (Now handled by .section-title) */
        /* CONSISTENCY: Margin-bottom now set to 30px to match global section-title margin */
        margin-bottom: 30px;
    }

    /* P is now using .consistent-paragraph-text class for consistent sizing */
    .legacy-section p {
        /* REMOVED: font-size, line-height, font-weight (Now handled by .consistent-paragraph-text) */
        /* CONSISTENCY: Margin-bottom is set to 25px to separate from the button */
        margin-bottom: 25px;
    }

    /* CTA Button (Design preserved) */
    .join-button {
        display: inline-flex;
        align-items: center;
        text-decoration: none;
        color: white;
        background-color: transparent;
        border: 2px solid white;
        /* CONSISTENCY: Preserve button size, slightly adjust padding to match if needed */
        padding: 10px 25px;
        border-radius: 50px;
        font-weight: 600;
        /* CONSISTENCY: Margin-top adjusted to 25px to match paragraph spacing */
        margin-top: 25px;
        transition:
            background-color 0.3s ease,
            color 0.3s ease;
    }

    /* --- MEDIA QUERIES (Harmonization and Fixes Applied) --- */

    /* Tablet and Small Desktop (max-width: 992px) */
    @media (max-width: 992px) {
        .legacy-section {
            /* CONSISTENCY: Vertical padding handled by .site-section global 992px rules */
            padding: 0 20px;
        }

        .legacy-section h1 {
            /* REMOVED: font-size (Now handled by .section-title global 992px rules) */
            margin-bottom: 30px;
        }

        .legacy-section p {
            /* REMOVED: font-size (Now handled by .consistent-paragraph-text global 992px rules) */
            margin-bottom: 25px;
        }
    }

    /* Mobile devices (max-width: 768px) */
    @media (max-width: 768px) {
        .legacy-section {
            /* CONSISTENCY: Vertical padding handled by .site-section global 768px rules */
            padding: 0 15px;
        }

        .legacy-section h1 {
            /* REMOVED: font-size (Now handled by .section-title global 768px rules) */
            /* CONSISTENCY: Margin-bottom harmonized */
            margin-bottom: 25px;
        }

        .legacy-section p {
            /* REMOVED: font-size (Now handled by .consistent-paragraph-text global 768px rules) */
            margin-bottom: 20px;
        }

        .join-button {
            /* CONSISTENCY: Button size harmonized */
            padding: 8px 20px;
            font-size: 14px; /* Set to 14px for consistency with global small text */
        }
    }

    /* Very small mobile devices (max-width: 480px) - **CHECKED & HARMONIZED** */
    @media (max-width: 480px) {
        .legacy-section {
            /* CONSISTENCY: Vertical padding handled by .site-section global 480px rules */
            padding: 0 10px;
        }

        .legacy-section h1 {
            /* REMOVED: font-size (Now handled by .section-title global 480px rules) */
            /* CONSISTENCY: Margin-bottom harmonized */
            margin-bottom: 20px;
        }

        .legacy-section p {
            /* REMOVED: font-size (Now handled by .consistent-paragraph-text global 480px rules) */
            margin-bottom: 15px;
        }

        .join-button {
            /* CONSISTENCY: Button size harmonized */
            padding: 8px 18px;
            font-size: 13px; /* Set to 13px for consistency with global smallest text */
        }
    }
</style>
