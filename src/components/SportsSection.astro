<section class="legacy-section site-section">
    <div class="legacy-content">
        <p 
            data-i18n="sports_legacy_title" 
            class="legacy-heading-text"
        >
            Tharlam Sports Association
        </p>
        <p
            data-i18n="sports_legacy_description"
            class="consistent-paragraph-text"
        >
            A core part of the **Tharlam Monastery's** commitment to holistic
            development, the Sports Association offers multiple programs,
            fostering **teamwork, discipline, and healthy living** among our
            students. Our primary goal is to nurture both academic and athletic
            potential within the monastic community.
        </p>
        <a href="/final/sport" class="join-button">
            <span data-i18n="sports_legacy_cta_text">View Our Programs</span>
            <span class="arrow">&rarr;</span>
        </a>
    </div>
</section>
<style>
    /* --- BASE STYLES (Desktop/Default) --- */

    .legacy-section {
        background-image: linear-gradient(
            rgba(0, 0, 0, 0.6),
            rgba(0, 0, 0, 0.6)
        ),
        url("/final/football.webp");
        background-size: cover;
        background-position: center;
        color: white;
        padding: 30px 20px;
        text-align: center;
    }

    .legacy-content {
        max-width: 900px;
        margin: 0 auto;
    }

    /* New style for the paragraph acting as a heading */
    .legacy-heading-text {
        font-size: 3em; /* Large size to mimic h1 */
        font-weight: 700; /* Bold */
        margin-bottom: 30px;
        line-height: 1.2;
    }

    /* The main description paragraph */
    .legacy-section p {
        margin-bottom: 25px;
    }

    /* CTA Button */
    .join-button {
        display: inline-flex;
        align-items: center;
        text-decoration: none;
        color: white;
        background-color: transparent;
        border: 2px solid white;
        padding: 10px 25px;
        border-radius: 50px;
        font-weight: 600;
        transition:
            background-color 0.3s ease,
            color 0.3s ease;
    }

    .join-button:hover {
        background-color: rgba(255, 255, 255, 0.15);
        color: white;
    }

    /* --- MEDIA QUERIES (Harmonized and Simplified) --- */

    /* Tablet and Small Desktop (max-width: 992px) */
    @media (max-width: 992px) {
        .legacy-section {
            padding: 0 20px;
        }

        .legacy-heading-text {
            font-size: 2em; /* Adjusted size for tablet */
            margin-bottom: 30px;
        }
    }

    /* Mobile devices (max-width: 768px) */
    @media (max-width: 768px) {
        .legacy-section {
            padding: 0 15px;
        }

        .legacy-heading-text {
            font-size: 2em; /* Smaller size for mobile */
            /* Enhancements for small screens */
            padding: 10px 0; 
            text-shadow: 0 1px 3px rgba(0, 0, 0, 0.7);
            /* ------------------------------------- */
            margin-bottom: 25px;
        }

        .legacy-section p {
            margin-bottom: 20px;
        }

        .join-button {
            padding: 8px 20px;
            font-size: 14px;
        }
    }

    /* Very small mobile devices (max-width: 480px) */
    @media (max-width: 480px) {
        .legacy-section {
            padding: 0 10px;
        }

        .legacy-heading-text {
            font-size: 1.5em; /* Smallest size */
            margin-bottom: 20px;
        }

        .legacy-section p {
            margin-bottom: 15px;
        }

        .join-button {
            padding: 8px 18px;
            font-size: 13px;
        }
    }
</style>